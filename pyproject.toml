[project]
name = "mcp-server-weather"
version = "0.1.0"
description = "MCP server providing weather information via wttr.in"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx",
    "mcp[cli]",
]
